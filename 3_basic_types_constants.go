/*
   【Constants】常量: 
		expressions whose evaluation is guaranteed to occur at compile time, not at run time.
	The underlying type of every constant is a【basic type】：boolean / string / number
*/

package main 

import (
	"fmt"
	"unicode/utf8"
)

func main() {
	/*
		The 
	*/
	s := "a和b"
	
	fmt.Println("\ufffd")
}